"use strict";

// count collector is not keys, count - each call should be new Array(count) each will be placed
// according to placement in collector + mapping to indexes

class Collector {
}

module.exports = Collector;